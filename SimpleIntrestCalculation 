#include <stdio.h>

int main() {
  float p,r,t,s;
  printf("enter the value of p,r and t");
  scanf("%f %f %f", &p,&r,&t);
  s=(p*r*t)/100;
  printf("simple intrest is %.2f",s);
  return 0;
}
//output 
enter the value of p,r and t10 20 30 
simple intrest is 60.00